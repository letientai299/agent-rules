# TypeScript Rules

- Most logic in `.ts` files (testable without UI runtime).
- Keep functions short, single responsibility, minimal arguments.
- Use modern DOM, JavaScript, TypeScript, CSS features.
